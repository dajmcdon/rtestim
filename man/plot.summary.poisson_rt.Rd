% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{plot.summary.poisson_rt}
\alias{plot.summary.poisson_rt}
\title{Plot predicted observed_count and estimated Rt from `summary(poisson_rt)` object}
\usage{
\method{plot}{summary.poisson_rt}(summary, ...)
}
\arguments{
\item{summary}{summary of `poisson_rt` models, of class `summary.poisson_rt`}

\item{...}{.}
}
\value{
Panel with two figures. Left figure shows the predicted observed_counts
calculated from \eqn{weighted_past_count * R} against the true observed_counts.
Right figure shows the estimated Rt across observation time
}
\description{
Plot predicted observed_count and estimated Rt from `summary(poisson_rt)` object
}
\examples{
y <- c(rev(seq(2, 6, by = 1)), seq(2, 6, by = 1))
mod <- estimate_rt(observed_counts = y, degree = 1, lambda = 0.001)
plot(summary(mod))
}
